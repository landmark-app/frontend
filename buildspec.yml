version: 0.2

phases:
  install:
    commands:
      - sudo yum install nodejs npm --enablerepo=epel
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
artifacts:
  base-directory: build/
  files:
    - '**/*'
